import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.espressotracker.app',
  appName: 'Espresso Tracker',
  webDir: 'dist'
};

export default config;
